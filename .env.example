# Required — get both from your WrenAI Cloud project (agentic projects only).
WREN_PROJECT_ID=123
WREN_API_KEY=sk-your-project-api-key

# Optional — defaults to the WrenAI Cloud API. Override for self-hosted deployments.
# WREN_API_BASE=https://cloud.getwren.ai/api/v2
